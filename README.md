# 🧮 CalcCLI - Интеллектуальный веб-калькулятор

Полнофункциональное приложение для вычисления математических выражений с поддержкой переменных, истории команд, встроенного AI-ассистента и веб-интерфейса.

## 📋 Описание

**CalcCLI** - это современный калькулятор на Go с веб-интерфейсом, который предоставляет:

- 🔢 **Математические вычисления** - поддержка сложных выражений с переменными
- 🤖 **AI-ассистент** - интеграция с DeepSeek для выполнения произвольных команд
- 📝 **История команд** - полное отслеживание всех вычислений
- 💾 **Сохранение состояния** - сохранение переменных и данных между сеансами
- 📊 **Мониторинг** - интеграция с Prometheus и Grafana для отслеживания метрик
- 🌐 **Веб-интерфейс** - современный интерфейс для взаимодействия
- 🐳 **Docker** - готовые контейнеры для развертывания
- 🔧 **WebRTC** - поддержка видеосвязи через Python сервер

## 🚀 Быстрый старт

### Предварительные требования

- **Go** 1.23.0 или выше
- **Docker** и **Docker Compose** (опционально, для контейнеризации)
- **Python 3.8+** (для WebRTC сервера)

### Локальный запуск

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd calcCLI

# 2. Загрузите зависимости
go mod download

# 3. Запустите приложение
go run main.go
```

Приложение автоматически откроет браузер на `http://localhost:8080`

### Запуск с Docker

```bash
# Запустите с Docker Compose (включает калькулятор, WebRTC, Prometheus и Grafana)
docker-compose up -d

# Доступ к приложению
# - Калькулятор: http://localhost:8080
# - WebRTC сервер: http://localhost:8000
# - Prometheus: http://localhost:9090
# - Grafana: http://localhost:3000
```

## 📁 Структура проекта

```
calcCLI/
├── main.go                    # Точка входа приложения
├── go.mod / go.sum            # Управление зависимостями
├── docker-compose.yml         # Конфигурация контейнеров
├── go-app.Dockerfile          # Dockerfile для Go приложения
│
├── config/                    # Конфигурация приложения
│   └── config.go             # Загрузка переменных окружения
│
├── core/                      # Основной функционал
│   ├── interpreter/           # Интерпретатор выражений
│   │   ├── interpreter.go
│   │   ├── interpreter_test.go
│   │   └── calculator_data.json  # Сохраненные данные
│   │
│   ├── evaluator/             # Вычисление математических выражений
│   │   ├── evaluator.go
│   │   └── evaluator_test.go
│   │
│   ├── ai/                    # AI-ассистент (DeepSeek)
│   │   ├── agent.go
│   │   └── agent_test.go
│   │
│   ├── applauncher/           # Запуск приложений
│   │   ├── applauncher.go
│   │   ├── applauncher_test.go
│   │   └── test_file_temp.txt
│   │
│   ├── curl/                  # HTTP-клиент
│   │   ├── curl_client.go
│   │   └── curl_client_test.go
│   │
│   ├── history/               # Управление историей
│   │   └── history_manager.go
│   │
│   ├── persistence/           # Сохранение данных
│   │   └── persistence.go
│   │
│   └── variables/             # Хранилище переменных
│       └── variable_store.go
│
├── ui/                        # Пользовательский интерфейс
│   └── web.go                # Веб-интерфейс (HTTP сервер)
│
├── metrics/                   # Метрики приложения
│   └── metrics.go            # Prometheus метрики
│
├── monitoring/                # Настройка мониторинга
│   ├── grafana/
│   │   ├── dashboards/
│   │   │   └── main-dashboard.json
│   │   └── provisioning/
│   │       ├── dashboards/
│   │       │   └── default.yml
│   │       └── datasources/
│   │           └── prometheus.yml
│   └── prometheus/
│       ├── alerts.yml
│       └── prometheus.yml
│
├── static/                    # Статические файлы
│   ├── index.html            # Главная страница
│   ├── script.js             # JavaScript логика
│   └── style.css             # Стили
│
└── app/                       # WebRTC приложение
    ├── server.py             # Python WebRTC сервер
    ├── index.html            # WebRTC интерфейс
    ├── requirements.txt      # Python зависимости
    └── webrtc.Dockerfile     # Dockerfile для WebRTC
```

## 🔧 Конфигурация

Создайте файл `.env` в корневой директории проекта:

```env
# Учетные данные
USER=your_username
PASSWORD=your_password

# AI-ассистент
DEEPSEEK_URL=http://deproxy.kchugalinskiy.ru/deeproxy/api

# Порты
CALC_PORT=8080
WEBRTC_PORT=8000
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
```

## 📚 Основные модули

### Interpreter
Главный модуль, обрабатывающий пользовательские команды:
- Классификация команд (вычисление, переменные, система, AI)
- Выполнение математических выражений
- Управление переменными
- Сохранение и загрузка состояния
- Обращение к AI-ассистенту

### Evaluator
Вычисляет математические выражения:
- Базовые операции (+ - * / %)
- Тригонометрические функции
- Логарифмические функции
- Работа с переменными

### VariableStore
Управление переменными:
- Сохранение/получение значений
- Валидация имен переменных
- Интеграция с persistence

### History Manager
Отслеживание истории:
- Сохранение всех команд
- Получение последних команд
- Очистка истории

### DeepSeek Agent
AI-интеграция:
- Обращение к API DeepSeek
- Обработка произвольных запросов
- Интеграция с системными командами

### Web Interface
Веб-сервер:
- REST API для всех операций
- Обслуживание статических файлов
- Prometheus метрики
- Health check эндпоинты

## 🌐 API Endpoints

### Вычисления
```
POST /api/calculate
Body: {"expression": "2 + 2 * 3"}
Response: {"result": "8"}
```

### Переменные
```
GET /api/variables          # Получить все переменные
POST /api/variables         # Создать переменную
DELETE /api/variables/:name # Удалить переменную
```

### История
```
GET /api/history           # Получить историю
GET /api/history/recent    # Последние команды
DELETE /api/history        # Очистить историю
```

### AI
```
POST /api/ai/execute
Body: {"prompt": "Выполни команду..."}
Response: {"result": "..."}
```

### Здоровье системы
```
GET /health                # Проверка доступности
GET /metrics               # Prometheus метрики
```

## 📊 Мониторинг

### Prometheus
- **URL**: http://localhost:9090
- **Конфиг**: `monitoring/prometheus/prometheus.yml`
- **Метрики приложения**:
  - `http_requests_total` - общее количество HTTP запросов
  - `http_request_duration_seconds` - длительность запросов
  - `calculation_duration_seconds` - длительность вычислений
  - `cache_hits_total` - попадания в кеш

### Grafana
- **URL**: http://localhost:3000
- **Dashboard**: `main-dashboard.json`
- **Default credentials**: `admin` / `admin`

## 🧪 Тестирование

Запустите все тесты:

```bash
go test ./...
```

Тесты по модулям:

```bash
# Тесты интерпретатора
go test ./core/interpreter -v

# Тесты вычислителя
go test ./core/evaluator -v

# Тесты AI-ассистента
go test ./core/ai -v

# И т.д. для других модулей
```

Скрипт для тестирования в PowerShell:

```powershell
./test.ps1
```

## 🔐 Безопасность

- ✅ Валидация всех входных данных
- ✅ Защита от инъекций в выражениях
- ✅ Аутентификация для API
- ✅ Health checks для всех сервисов
- ✅ Логирование всех операций

## 📈 Производительность

- Кеширование результатов вычислений
- Оптимизация вычисления выражений
- Асинхронные операции где возможно
- Мониторинг производительности через Prometheus

## 🛠️ Разработка

### Добавление новой функции

1. Создайте новый модуль в `core/`
2. Реализуйте функциональность
3. Добавьте тесты (`*_test.go`)
4. Интегрируйте в `interpreter.go`
5. Добавьте API эндпоинт в `ui/web.go`

### Локальная разработка с Docker

```bash
# Пересоберите контейнер при изменениях
docker-compose down
docker-compose up --build
```
